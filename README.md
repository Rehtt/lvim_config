# USE
```bash
cd ~/.config/ && mv lvim lvim_bk
mkdir lvim && cd lvim

git init
git remote add origin https://github.com/Rehtt/lvim_config.git
git branch --set-upstream-to=origin/master master
git pull
```
